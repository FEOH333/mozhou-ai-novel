# 更新日志

本文件记录墨舟所有值得用户注意的变更，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

**怎么维护（三条就够）**

1. 改动面向用户 → 在 `## [未发布]` 下加一条，类别用 `新增 / 变更 / 修复 / 移除 / 安全`。
2. 发版本时把 `[未发布]` 改成 `## [x.y.z] - YYYY-MM-DD`，并在文件顶部补一条空的 `[未发布]`。
3. 写"用户能感知到什么"，不写"改了哪个函数"。内部重构若用户无感，归到该版本的 `变更` 里一句带过。

> 早期版本（V0.100.9 之前）的开发历史未随仓库迁移保留，本文件从可获得的最早版本开始记录。

---

## [未发布]

### 新增

- **紧急完本**：可从任意进度一键收束——系统规划一个收束卷集中兑付未回收伏笔，写完即完本，不再受 20 万字下限与 500 章上限约束。
- **续作（衍新书）**：以已完本作品为母本开新书，继承世界观、设定与事实库，可挑选继承角色，重新立契约与主线。
- **应用内更新日志页**：顶部导航新增「更新日志」，直接查看本文件内容，无需离开应用。
- **`AGENTS.md` 项目地图**：面向 AI 开发者的导航（去哪找、改什么、别踩什么），显著降低 AI 协助开发的探索成本。

### 变更

- **项目结构重组**：`server/engine/` 从 59 个文件平铺改为按职责域分 7 个子目录
  （`pipeline/` `planning/` `quality/` `narrative/` `longform/` `recovery/` + 根级 `prompts.js`），
  目录名即导航。功能与行为完全不变。

---

## [0.109.3] - 2026-09-11

### 新增

- **通用中文 AI 腔防线**：新增一类独立于"网文套话"的机器腔检测，覆盖抽象黑话与空洞大词、
  翻译腔框架、单句多重"的"、叙述层顿悟升华收束、"带着一丝X"式万能状语、三句等长同字起头的排比凑势、
  整章句式板结、连接词硬接逻辑、通篇无情绪落点或具体时间数目地名。共 11 个本地检测器，
  命中后进入审校与修订自愈，写作、审校、修订、打磨四处共用同一把尺。

### 变更

- **issue 类型语义收敛为单一真源**（`server/data/issue_types.js`）。此前「哪些类型可修、哪些记债」
  硬编码在 5 处消费点，新增一类纪律必须同时改全这 5 处，否则命中不触发修订自愈、检测器形同虚设。
  现在新增纪律只需登记一次。

### 修复

- 三段式排比检测原先会误伤正常叙事（中文排比是正当修辞），改为要求"等长 + 同字起头"机器签名才报。
- 对白内的"的"字地狱豁免原先失效（引号常开在句中，按句首位置判定必然漏判）。

---

## [0.109.2] - 2026-09-10

### 修复

- **本地向量模型缓存损坏时自动自愈，不再永久降级**。此前缓存文件损坏与网络失败抛同一句话，
  换任何模型源都无效（都读同一个坏文件），最终静默退化为关键词检索且用户无法自行恢复。

---

## [0.109.1] - 2026-09-10

### 安全

- **API Key 支持从环境变量注入，磁盘不再留明文**（`NOVEL_API_KEY` / `NOVEL_BACKUP_API_KEY`）。
  保存设置时不会把环境变量接管的值写回 `config.json`。

---

## [0.109.0] - 2026-09-08

### 新增

- **API 动态编排**：主端点卡顿/熔断/失效时自动切换备用端点，主端点恢复后探针自动回切；
  请求内快切 + 粘性窗口 + 60 秒探针回切，备用连续失败自身熔断以防双端点循环打转。
- 设置页双通道改版。

---

## [0.108.0] - 2026-09-03

### 新增

- **人物活性工艺**：对手与反派的行动必须能从其自身立场/利益/目标解释，不得无脑针对主角；
  配角保留自己的生活线，不为送剧情而存在。角色卡新增"他的立场""退路""动机根源"等字段。

---

## [0.107.0] - 2026-08-31

### 新增

- **章卷命名工艺**：章名承诺的事件必须在正文真实落地或直接触及（转述、塘报式落地算触及），
  章名与内容牛头不对马嘴会被判为题眼级事故。
- 分章科学化。

---

## [0.105.0] - 2026-08-26

### 新增

- **近窗发稿占用轴**：按近章实际占用情况点名提示，不把发稿自查做成每章考卷。
- **阶段关卡窗**与**卷缝体检收口**：写下一卷前先体检上一卷的实际出口，避免承接断裂。
- **名场面工艺**（V0.105.1）：卷纲阶段策划高潮，释放要定格成读者一句话能转述的画面。

---

## [0.104.0] - 2026-08-24

### 新增

- **观察驾驶舱**：自动创作与浏览器解绑，断线/刷新不打断后台作业，可按作业 ID 重放进度。

---

## [0.101.0] - 2026-08-20

### 新增

- **自动创作质量内核**：写时配额、角色语音卡、低谷调度与题材偏好槽。

---

## [0.100.0] - 2026-08-13

### 新增

- **推荐评估返工闭环**：以平台推荐评估为目标的候选返工——逐章诊断、工单化改写、
  两次 A/B 换位盲审、整段复核后才落盘；候选在影子版本中全部验证通过才切换，失败保留整套旧稿。

---

[未发布]: https://github.com/FEOH333/mozhou-ai-novel/compare/v0.109.3...HEAD
[0.109.3]: https://github.com/FEOH333/mozhou-ai-novel/releases/tag/v0.109.3
